{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WCAG Contrast Audit Inputs",
  "description": "Input parameters for the WCAG Contrast Audit AI Skill Package",
  "type": "object",
  "properties": {
    "target_html": {
      "type": "string",
      "description": "Relative path to the HTML file to audit",
      "default": "index.html"
    },
    "level": {
      "type": "string",
      "enum": ["AA", "AAA", "both"],
      "description": "WCAG conformance level to test against",
      "default": "AA"
    },
    "output": {
      "type": "string",
      "enum": ["table", "json", "csv"],
      "description": "Output format for audit results",
      "default": "table"
    }
  },
  "required": []
}
